---
import { config } from '../../astro.config.mjs';

let { href, ...props } = Astro.props;
console.log(href);
href = `/${config.base}/${href}`.replace(/\/\//g, '/');
---
<a {...props} href={href}><slot></slot></a>